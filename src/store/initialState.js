const stateData = {
    compType : 'default',
    listView: [],
    singleView : {}
}

//const testing = objectPath.del(stateData.components, [0, 'col1', 1]);

//console.log(testing)

export default stateData
